const result = Math.pow(3, 8);
console.log(result);

const num1 = 25;
const num2 = 45;

const gap = Math.abs(num1 - num2);
if (gap < 5) {
    console.log('Now you are friend');
}
else {
    console.log('We are not friend');
}

const number = 2.5647;
const fullNumber = Math.round(number);
console.log(fullNumber);
